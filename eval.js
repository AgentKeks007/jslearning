eval('console.log(1+2)');

let arr = [0, 5, 1, 2, 4];
console.log(arr.join('+'));
/*arr.join('+').forEach(element => {
    console.log(element);
});*/
for(let i = 0; i < arr.length-1; i++)
{
    console.log(i);
}